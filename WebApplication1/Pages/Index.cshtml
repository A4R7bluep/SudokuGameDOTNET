@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

@functions {
    string get_styles(string spaceX, string spaceY)
    {
        

        return "background: black;";
    }

    public class Nonet
    {
        private int[,] area = {
            {0, 0, 0}, //    1a, 2a, 3a
            {0, 0, 0}, //    1b, 2b, 3b
            {0, 0, 0}  //    1c, 2c, 3c
        };

        public void set_space_value(string space, int value)
        {
            switch (space)
            {
                case "1a":
                    area[0, 0] = value;
                    break;

                case "2a":
                    area[0, 1] = value;
                    break;

                case "3a":
                    area[0, 2] = value;
                    break;

                case "1b":
                    area[1, 0] = value;
                    break;

                case "2b":
                    area[1, 1] = value;
                    break;

                case "3b":
                    area[1, 2] = value;
                    break;

                case "1c":
                    area[2, 0] = value;
                    break;

                case "2c":
                    area[2, 1] = value;
                    break;

                case "3c":
                    area[2, 2] = value;
                    break;
            }
        }

        public int get_space_value(int spaceX, int spaceY)
        {
            return area[spaceX, spaceY];
        }
    }

    public class Board
    {
        private Nonet[,] main = { { null, null, null }, { null, null, null }, { null, null, null } };

        public Board()
        {
            for (int y = 0; y < 3; y++)
            {
                for (int x = 0; x < 3; x++)
                {
                    main[x, y] = new Nonet();
                }
            }
        }

        //    1a, 2a, 3a
        //    1b, 2b, 3b
        //    1c, 2c, 3c

        public string get_nonet_value(int space1, int space2)
        {
            Nonet currentNonet = null;

            currentNonet = main[space1 / 3, space2 / 3];
            string value = (currentNonet.get_space_value(space1 % 3, space2 % 3)).ToString();

            return value;
        }
    }
}

@{
    Board board = new Board();

    string Send()
    {
        Console.WriteLine(board);
        return "";
    }

    string main = Send();
}

<!--
<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>
-->

<table id="board">
    <tr id="row1">
        <td class="top_border left_border" style="@get_styles("a", "a")><button>@board.get_nonet_value(0, 0)</button></td>
        <td class="top_border"><button>@board.get_nonet_value(0, 1)</button></td>
        <td class="top_border right_border"><button>@board.get_nonet_value(0, 2)</button></td>
        <td class="top_border"><button>@board.get_nonet_value(0, 3)</button></td>
        <td class="top_border"><button>@board.get_nonet_value(0, 4)</button></td>
        <td class="top_border"><button>@board.get_nonet_value(0, 5)</button></td>
        <td class="top_border left_border"><button>@board.get_nonet_value(0, 6)</button></td>
        <td class="top_border"><button>@board.get_nonet_value(0, 7)</button></td>
        <td class="top_border right_border"><button>@board.get_nonet_value(0, 8)</button></td>
    </tr>
    <tr id="row2">
        <td class="left_border"><button>@board.get_nonet_value(1, 0)</button></td>
        <td><button>@board.get_nonet_value(1, 1)</button></td>
        <td class="right_border"><button>@board.get_nonet_value(1, 2)</button></td>
        <td><button>@board.get_nonet_value(1, 3)</button></td>
        <td><button>@board.get_nonet_value(1, 4)</button></td>
        <td><button>@board.get_nonet_value(1, 5)</button></td>
        <td class="left_border"><button>@board.get_nonet_value(1, 6)</button></td>
        <td><button>@board.get_nonet_value(1, 7)</button></td>
        <td class="right_border"><button>@board.get_nonet_value(1, 8)</button></td>
    </tr>
    <tr id="row3">
        <td class="left_border bottom_border"><button>@board.get_nonet_value(2, 0)</button></td>
        <td class="bottom_border"><button>@board.get_nonet_value(2, 1)</button></td>
        <td class="bottom_border right_border"><button>@board.get_nonet_value(2, 2)</button></td>
        <td class="bottom_border"><button>@board.get_nonet_value(2, 3)</button></td>
        <td class="bottom_border"><button>@board.get_nonet_value(2, 4)</button></td>
        <td class="bottom_border right_border"><button>@board.get_nonet_value(2, 5)</button></td>
        <td class="bottom_border"><button>@board.get_nonet_value(2, 6)</button></td>
        <td class="bottom_border"><button>@board.get_nonet_value(2, 7)</button></td>
        <td class="bottom_border right_border"><button>@board.get_nonet_value(2, 8)</button></td>
    </tr>
    <tr id="row4">
        <td class="left_border"><button>@board.get_nonet_value(3, 0)</button></td>
        <td><button>@board.get_nonet_value(3, 1)</button></td>
        <td><button>@board.get_nonet_value(3, 2)</button></td>
        <td class="left_border"><button>@board.get_nonet_value(3, 3)</button></td>
        <td><button>@board.get_nonet_value(3, 4)</button></td>
        <td><button>@board.get_nonet_value(3, 5)</button></td>
        <td class="left_border"><button>@board.get_nonet_value(3, 6)</button></td>
        <td><button>@board.get_nonet_value(3, 7)</button></td>
        <td class="right_border"><button>@board.get_nonet_value(3, 8)</button></td>
    </tr>
    <tr id="row5">
        <td class="left_border"><button>@board.get_nonet_value(4, 0)</button></td>
        <td><button>@board.get_nonet_value(4, 1)</button></td>
        <td><button>@board.get_nonet_value(4, 2)</button></td>
        <td class="left_border"><button>@board.get_nonet_value(4, 3)</button></td>
        <td><button>@board.get_nonet_value(4, 4)</button></td>
        <td><button>@board.get_nonet_value(4, 5)</button></td>
        <td class="left_border"><button>@board.get_nonet_value(4, 6)</button></td>
        <td><button>@board.get_nonet_value(4, 7)</button></td>
        <td class="right_border"><button>@board.get_nonet_value(4, 8)</button></td>
    </tr>
    <tr id="row6">
        <td class="left_border bottom_border"><button>@board.get_nonet_value(5, 0)</button></td>
        <td class="bottom_border"><button>@board.get_nonet_value(5, 1)</button></td>
        <td class="bottom_border"><button>@board.get_nonet_value(5, 2)</button></td>
        <td class="bottom_border left_border"><button>@board.get_nonet_value(5, 3)</button></td>
        <td class="bottom_border"><button>@board.get_nonet_value(5, 4)</button></td>
        <td class="bottom_border"><button>@board.get_nonet_value(5, 5)</button></td>
        <td class="bottom_border left_border"><button>@board.get_nonet_value(5, 6)</button></td>
        <td class="bottom_border"><button>@board.get_nonet_value(5, 7)</button></td>
        <td class="bottom_border right_border"><button>@board.get_nonet_value(5, 8)</button></td>
    </tr>
    <tr id="row7">
        <td class="left_border"><button>@board.get_nonet_value(6, 0)</button></td>
        <td><button>@board.get_nonet_value(6, 1)</button></td>
        <td><button>@board.get_nonet_value(6, 2)</button></td>
        <td class="left_border"><button>@board.get_nonet_value(6, 3)</button></td>
        <td><button>@board.get_nonet_value(6, 4)</button></td>
        <td><button>@board.get_nonet_value(6, 5)</button></td>
        <td class="left_border"><button>@board.get_nonet_value(6, 6)</button></td>
        <td><button>@board.get_nonet_value(6, 7)</button></td>
        <td class="right_border"><button>@board.get_nonet_value(6, 8)</button></td>
    </tr>
    <tr id="row8">
        <td class="left_border"><button>@board.get_nonet_value(7, 0)</button></td>
        <td><button>@board.get_nonet_value(7, 1)</button></td>
        <td><button>@board.get_nonet_value(7, 2)</button></td>
        <td class="left_border"><button>@board.get_nonet_value(7, 3)</button></td>
        <td><button>@board.get_nonet_value(7, 4)</button></td>
        <td><button>@board.get_nonet_value(7, 5)</button></td>
        <td class="left_border"><button>@board.get_nonet_value(7, 6)</button></td>
        <td><button>@board.get_nonet_value(7, 7)</button></td>
        <td class="right_border"><button>@board.get_nonet_value(7, 8)</button></td>
    </tr>
    <tr id="row9">
        <td class="bottom_border left_border"><button>@board.get_nonet_value(8, 0)</button></td>
        <td class="bottom_border"><button>@board.get_nonet_value(8, 1)</button></td>
        <td class="bottom_border"><button>@board.get_nonet_value(8, 2)</button></td>
        <td class="bottom_border left_border"><button>@board.get_nonet_value(8, 3)</button></td>
        <td class="bottom_border"><button>@board.get_nonet_value(8, 4)</button></td>
        <td class="bottom_border"><button>@board.get_nonet_value(8, 5)</button></td>
        <td class="bottom_border left_border"><button>@board.get_nonet_value(8, 6)</button></td>
        <td class="bottom_border"><button>@board.get_nonet_value(8, 7)</button></td>
        <td class="bottom_border right_border"><button>@board.get_nonet_value(8, 8)</button></td>
    </tr>

</table>

<style>
    table {
        border: 1px solid black;
    }

    td {
        border: 1px solid black;
        padding-top: 20px;
        padding-bottom: 20px;
        padding-right: 25px;
        padding-left: 25px;
    }

    button {
        border: none;
        background-color: white;
    }

    .bottom_border {
        border-bottom: 3px solid black;
    }

    .right_border {
        border-right: 3px solid black;
    }

    .top_border {
        border-top: 3px solid black;
    }

    .left_border {
        border-left: 3px solid black;
    }
</style>